<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 
        Base application theme for Activity
        The actual Material3 theming is done in Compose via SmartLightingTheme
    -->
    <style name="Theme.SmartLighting" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Transparent background - Compose handles the UI -->
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>

